spring:
  datasource:
    url: jdbc:tc:postgresql:15.5:///liuyao
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

liuyao:
  rules:
    version: v1
  llm:
    enabled: false
